{
  "name": "spamtel-android",
  "version": "1.0.0",
  "description": "Blokada spamu telefonicznego - aplikacja Android",
  "scripts": {
    "build": "npm install && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {},
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}